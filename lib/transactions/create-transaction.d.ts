import { SignedTransaction, RootTransaction } from '../contracts/transaction';
declare const _default: (value: number, previousTransaction: SignedTransaction | RootTransaction, privateKey: string, passphrase: string, reference?: string | undefined) => SignedTransaction;
export default _default;
