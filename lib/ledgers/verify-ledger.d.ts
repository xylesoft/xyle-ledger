import { SignedTransaction } from '../contracts/transaction';
declare const _default: (transactions: SignedTransaction[], publicKey: string, privateKey: string, passphrase: string) => boolean;
export default _default;
